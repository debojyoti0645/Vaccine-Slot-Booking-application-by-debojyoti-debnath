<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="@drawable/nrmbtnbackground"
    android:layout_marginBottom="20dp">

    <TextView
        android:id="@+id/tvSlotInfo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Slot Information"
        android:textSize="16sp"
        android:textColor="@android:color/black" />

    <!-- TextView for Available status (green) -->
    <TextView
        android:id="@+id/tvStatusAvailable"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="16sp"
        android:text="Available"
        android:textColor="@android:color/holo_green_dark"
        android:visibility="gone" /> <!-- Initially hidden -->

    <!-- TextView for Cancelled status (red) -->
    <TextView
        android:id="@+id/tvStatusCancelled"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="16sp"
        android:text="Cancelled"
        android:textColor="@android:color/holo_red_dark"
        android:visibility="gone" /> <!-- Initially hidden -->

</LinearLayout>
